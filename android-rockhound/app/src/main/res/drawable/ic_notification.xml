<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">
    <!--
        RockScout crystal app icon — white silhouette on a transparent background,
        used as the notification small icon. The system applies the notification
        accent tint, so the source must remain single-color (white on transparent).
    -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M12,2 L18,6.5 L18,11.5 L12,22 L6,11.5 L6,6.5 Z" />
</vector>
